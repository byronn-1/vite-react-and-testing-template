import ExampleDetail from 'exampleFeature/components/detail/ExampleDetail';

/**
 * See https://react-location.tanstack.com for how to add routes here.
 */
const routes = {
  path: 'exampleFeature',
  element: <ExampleDetail />
};

export default routes;
